version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --prefix project
    build:
      commands:
        - npm run build --prefix project
  artifacts:
    baseDirectory: project/dist
    files:
      - '**/*'
  cache:
    paths:
      - project/node_modules/**/*
