{
  "name": "fullcam-api-proxy-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda proxy for FullCAM API",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && npm ci --production && powershell Compress-Archive -Path node_modules,index.js -DestinationPath lambda.zip -Force"
  },
  "dependencies": {
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.2.2"
  }
}
